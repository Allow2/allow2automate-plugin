{
  "name": "{{PLUGIN_NAME}}",
  "version": "1.0.0",
  "description": "{{DESCRIPTION}}",
  "main": "index.js",
  "author": "{{AUTHOR_NAME}} <{{AUTHOR_EMAIL}}>",
  "license": "MIT",
  "keywords": [
    "allow2automate",
    "plugin",
    "automation",
    "{{KEYWORD_1}}",
    "{{KEYWORD_2}}"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/{{GITHUB_USERNAME}}/{{PLUGIN_NAME}}.git"
  },
  "bugs": {
    "url": "https://github.com/{{GITHUB_USERNAME}}/{{PLUGIN_NAME}}/issues"
  },
  "homepage": "https://github.com/{{GITHUB_USERNAME}}/{{PLUGIN_NAME}}#readme",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write \"**/*.{js,json,md}\"",
    "typecheck": "tsc --noEmit",
    "prepare": "npm run lint && npm run test"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.0.0",
    "jest": "^29.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "react": ">=18.0.0"
  },
  "files": [
    "index.js",
    "src/",
    "README.md",
    "LICENSE"
  ],
  "allow2automate": {
    "plugin": true,
    "version": "1.0.0",
    "compatibility": ">=1.0.0"
  }
}
